const adminProfile=require("../../model/amdmin_model/adminProfile");
const adminAuthRepo={};
adminAuthRepo.read=async(email,password)=>{
    try {
        
    } catch (error) {
        console.log(error);
        throw error;
    }
}